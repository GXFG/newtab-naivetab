# 沟通与协作规则

- 所有输出（对话、工具描述、注释、文档）**必须使用中文**。
- 遇到不清楚的点**不要猜测**，及时与用户确认。
- 修改代码前**先给具体方案让用户审阅**。涉及多文件、多方案选型、或架构决策时，使用 `EnterPlanMode` 编写完整方案；简单修改（单文件、方案明确）可直接口头描述后动手。
- **规范优先于直觉：** `.claude/rules/` 中的编码规范（如 i18n 格式、CSS 写法等）即使与框架默认语法不同，也必须严格遵守。看到规范后不要走直觉路径回到熟悉但错误的写法。
